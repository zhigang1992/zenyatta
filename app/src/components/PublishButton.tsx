import React from "react";

const PublishButton = () => {
  return (
    <button
      className="nav-btn"
      onClick={() => {
        // TODO
      }}
    >
      Publish
    </button>
  );
};

export default PublishButton;
